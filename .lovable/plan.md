

## Remove Redundant Backup Subtitle Text

**`src/components/settings/BackupRestoreSection.tsx`** — Remove the second sentence "Automatic snapshots run twice daily. You can also create one manually." from the subtitle paragraph, keeping only the quirky diamond line.

