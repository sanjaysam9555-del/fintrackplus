
The user sees a "Subscriptions are only available on the live app" message on the preview URL. They didn't navigate manually — Settings → Subscription took them there. They want to know why.

Let me check Billing.tsx for the gate logic.
